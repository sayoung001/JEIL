# 실행 필수
PySide6>=6.6
PyYAML>=6.0
openpyxl>=3.1        # .xlsx 읽기 전용
xlrd==2.0.1          # .xls 읽기 전용 (레거시 4개 파일)
xlwings>=0.31        # 쓰기 전용 (Windows + Excel 필수)
Pillow>=10.0         # 사진 압축 · EXIF 제거
PyMuPDF>=1.24        # 성적서 PDF 글자 추출 (스캔본은 OCR용 렌더링도 담당)

# 선택 — 없어도 성적서 PDF 는 읽힌다 (텍스트 레이어가 있어서)
# 스캔본·사진에서 글자를 읽어야 할 때만 설치한다.
# rapidocr-onnxruntime>=1.3    # 권장: pip 만으로 끝난다 (별도 프로그램 설치 불필요)
# pillow-heif>=0.16            # HEIC 사진을 직접 다룰 때

# 노트북 (VS Code 에서 셀 단위로 쓸 때)
# VS Code 가 처음 열 때 설치를 물어보므로 미리 넣지 않아도 된다.
# ipykernel>=6.29

# 빌드
pyinstaller>=6.0
